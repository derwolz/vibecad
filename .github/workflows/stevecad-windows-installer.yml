name: SteveCAD Windows Build Validation

on:
  workflow_dispatch:
    inputs:
      source_ref:
        description: "Branch, tag, or commit to validate."
        required: true
        default: "dev"
        type: string

permissions:
  contents: read

concurrency:
  group: stevecad-windows-validation-${{ inputs.source_ref }}
  cancel-in-progress: false

jobs:
  metadata:
    name: Resolve Canonical Metadata
    runs-on: ubuntu-24.04
    outputs:
      build: ${{ steps.meta.outputs.build }}
      build_tag: ${{ steps.meta.outputs.build_tag }}
      release_version: ${{ steps.meta.outputs.release_version }}
      release_title: ${{ steps.meta.outputs.release_title }}
      source_sha: ${{ steps.meta.outputs.source_sha }}
    steps:
      - name: Check out requested source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ inputs.source_ref }}
          fetch-depth: 1
          submodules: false

      - name: Resolve version and build
        id: meta
        shell: bash
        run: |
          set -euo pipefail
          python src/Tools/sync_version.py --check
          source_sha="$(git rev-parse --verify HEAD)"
          build="$(python src/Tools/resolve_release_artifact_name.py . --component build)"
          build_tag="$(python src/Tools/resolve_release_artifact_name.py . --component release-tag)"
          release_version="$(python src/Tools/resolve_release_artifact_name.py . --component release-version)"
          release_title="$(python src/Tools/resolve_release_artifact_name.py . --component release-title)"
          {
            echo "build=${build}"
            echo "build_tag=${build_tag}"
            echo "release_version=${release_version}"
            echo "release_title=${release_title}"
            echo "source_sha=${source_sha}"
          } >> "${GITHUB_OUTPUT}"
          echo "Validating ${release_title}" >> "${GITHUB_STEP_SUMMARY}"

  windows:
    name: Build Windows Bundle and Installer
    needs: metadata
    runs-on: windows-2022
    steps:
      - name: Check out source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ needs.metadata.outputs.source_sha }}
          fetch-depth: 0
          submodules: recursive

      - name: Install Pixi
        shell: bash
        run: |
          set -euo pipefail
          curl -fsSL https://pixi.sh/install.sh | bash
          echo "${HOME}/.pixi/bin" >> "${GITHUB_PATH}"

      - name: Restore Pixi and Python caches
        id: cache-pixi-windows
        uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: |
            package/rattler-build/.pixi
            package/rattler-build/.download-cache
            ~\AppData\Local\pixi
            ~\AppData\Local\rattler
            ~\AppData\Local\pip\Cache
          key: pixi-windows-${{ hashFiles('package/rattler-build/pixi.lock', 'package/rattler-build/pixi.toml', 'package/rattler-build/recipe.yaml', 'src/Mod/SteveCAD/requirements.txt', 'src/Mod/SteveCADAero/requirements-aero.txt', 'package/rattler-build/scripts/install_stevecad_provider_deps.sh', 'package/rattler-build/scripts/install_stevecad_codex_runtime.sh', 'package/rattler-build/scripts/purge_stevecad_retired_authoring_artifacts.sh') }}
          restore-keys: |
            pixi-windows-

      - name: Restore ccache
        uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ${{ runner.temp }}\ccache
          key: ccache-windows-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            ccache-windows-

      - name: Build package environment
        shell: bash
        working-directory: package/rattler-build
        env:
          CCACHE_DIR: ${{ runner.temp }}\ccache
          CCACHE_MAXSIZE: "2G"
        run: |
          set -euo pipefail
          if [[ -d .pixi/envs/default ]]; then
            pixi reinstall -e default stevecad
          else
            pixi install -e default
          fi
          pixi install -e package

      - name: Save ccache
        if: always()
        uses: actions/cache/save@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ${{ runner.temp }}\ccache
          key: ccache-windows-${{ github.run_id }}-${{ github.run_attempt }}

      - name: Create Windows bundle and installer
        shell: bash
        working-directory: package/rattler-build
        env:
          BUILD_TAG: ${{ needs.metadata.outputs.build_tag }}
          MAKE_INSTALLER: "true"
          MAKE_PORTABLE_ARCHIVE: "false"
        run: |
          set -euo pipefail
          pixi run -e package create_bundle

      - name: Verify clean installer upgrade from the previous build
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: >-
          ./package/WindowsInstaller/test_clean_upgrade.ps1
          -CurrentInstaller "package/rattler-build/windows/SteveCAD-${{ needs.metadata.outputs.release_version }}-build${{ needs.metadata.outputs.build }}-Windows-x86_64-installer.exe"
          -Repository "${{ github.repository }}"
          -ReleaseVersion "${{ needs.metadata.outputs.release_version }}"
          -Build ${{ needs.metadata.outputs.build }}

      - name: Save Pixi and Python caches
        if: always() && steps.cache-pixi-windows.outputs.cache-hit != 'true'
        uses: actions/cache/save@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: |
            package/rattler-build/.pixi
            package/rattler-build/.download-cache
            ~\AppData\Local\pixi
            ~\AppData\Local\rattler
            ~\AppData\Local\pip\Cache
          key: ${{ steps.cache-pixi-windows.outputs.cache-primary-key }}

      - name: Upload validation artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: stevecad-windows-${{ needs.metadata.outputs.build_tag }}
          if-no-files-found: error
          retention-days: 14
          path: |
            package/rattler-build/windows/*.exe
            package/rattler-build/windows/*-installer.exe-SHA256.txt
